-- Ticket #21 (walking skeleton): placeholder migration proving auto-migrate-on-boot works.
-- Real schema (Organization, entities, etc.) lands in ticket #22.

-- +goose Up
SELECT 1;

-- +goose Down
SELECT 1;
